import {
  expect
} from 'chai'

import Password from '#pinion/components/cogs/password'

describe('#pinion/components/cogs/password', () => {
  it('is a function', () => {
    expect(Password)
      .to.be.a('function')
  })
})
