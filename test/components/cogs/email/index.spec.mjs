import {
  expect
} from 'chai'

import Email from '#pinion/components/cogs/email'

describe('#pinion/components/cogs/email', () => {
  it('is a function', () => {
    expect(Email)
      .to.be.a('function')
  })
})
