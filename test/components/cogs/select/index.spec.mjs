import {
  expect
} from 'chai'

import Select from '#pinion/components/cogs/select'

describe('#pinion/components/cogs/select', () => {
  it('is a function', () => {
    expect(Select)
      .to.be.a('function')
  })
})
