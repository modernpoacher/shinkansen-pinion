import {
  expect
} from 'chai'

import Radio from '#pinion/components/cogs/radio'

describe('#pinion/components/cogs/radio', () => {
  it('is a function', () => {
    expect(Radio)
      .to.be.a('function')
  })
})
