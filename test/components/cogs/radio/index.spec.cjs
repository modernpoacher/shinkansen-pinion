const {
  expect // @ts-ignore
} = require('chai')

const Radio = require('#pinion/components/cogs/radio')

describe('#pinion/components/cogs/radio', () => {
  it('is a function', () => {
    expect(Radio)
      .to.be.a('function')
  })
})
