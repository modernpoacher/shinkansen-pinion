import {
  expect
} from 'chai'

import Textarea from '#pinion/components/cogs/textarea'

describe('#pinion/components/cogs/textarea', () => {
  it('is a function', () => {
    expect(Textarea)
      .to.be.a('function')
  })
})
