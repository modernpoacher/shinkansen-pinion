import {
  expect
} from 'chai'

import Checkbox from '#pinion/components/cogs/checkbox'

describe('#pinion/components/cogs/checkbox', () => {
  it('is a function', () => {
    expect(Checkbox)
      .to.be.a('function')
  })
})
