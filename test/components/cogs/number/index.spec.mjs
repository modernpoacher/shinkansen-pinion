import {
  expect
} from 'chai'

import Number from '#pinion/components/cogs/number'

describe('#pinion/components/cogs/number', () => {
  it('is a function', () => {
    expect(Number)
      .to.be.a('function')
  })
})
