import debug from 'debug'

import {
  FieldsetSprocket
} from 'shinkansen-sprockets'

const log = debug('shinkansen-pinion/components/sprockets/fieldset')

log('`shinkansen` is awake')

export default FieldsetSprocket
